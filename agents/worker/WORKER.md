# WORKER
Агент устранения ошибки.
Pipeline работы:
    - Получает причину проблемы от Diagnostic.
    - Строит план устранения проблемы
    - Исполняет план с помощью executer_mcp.
    - Повторно вызывает Diagnostic для проверки устранения проблемы.
